import { StyleSheet } from "react-native";
import { primaryLight } from "../../constants/theme.constant";

const styles = StyleSheet.create({
  input: {
    minWidth: 200,
    height: 40,
    margin: 10,
    padding: 10,
    borderWidth: 1,
    borderRadius: 50,
    borderColor: primaryLight,
  },
});
export default styles;
